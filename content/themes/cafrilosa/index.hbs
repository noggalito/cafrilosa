{{!< default}}
<section class="cover">
  <div class="container">
    <figure class="cover-brand col-xs-12">
      <img src="{{@blog.logo}}" alt="{{@blog.title}}"/>
      <hr>
      <figcaption class="col-xs-12 lead">{{@blog.description}}</figcaption>
    </figure>
    <figure class="cover-image">
      <img src="{{@blog.cover}}" alt="{{@blog.title}}" />
    </figure>
    {{> socialChannels}}
  </div>
</section>
{{> searchBox}}

<section>
  {{#foreach posts}}
    {{#if @first}}
  <a href="{{url}}">
    <div class="newPost" style="background-image:url({{image}});">
      <div class="container">
        <article class="newPost-info">
          <h3>Novedades</h3>
          <h4>{{{title}}}</h4>
        </article>
      </div>
    </div>
  </a>
    {{/if}}
  {{/foreach}}
</section>

<section class="productionLines">
  <div class="container">
    <div class="row">
      <article class="col-xs-12 col-sm-8 text-center">
        <h2>Nuestras líneas de producción</h2>
        <hr>
        <p class="col-sm-9">
          Contamos con 3 lineas de producción. Perfectas para cada ocación para disfrutar con amigos, familia y en eventos especiales. Pero no te quedes solamente con nuestras líneas de producción. Tenemos muchos más productos.
        </p>
        <a href="#" class="btn btn-primary" role="button">Ver Productos</a>
      </article>
    </div>
  </div>
</section>
